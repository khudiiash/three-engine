# Engine development invariants

## Response style

Respond short: main thesis first, 2-3 sentences with the key information. Detail belongs in plan docs and memory, not in chat replies.

## WebGPU GI binding budget

- Every GI compute shader must stay within WebGPU's portable limit of **8 storage buffers per shader stage** (`maxStorageBuffersPerShaderStage = 8`).
- Do not fix binding validation errors by requesting a device limit of 16. The engine must continue to run on adapters that expose only the portable default.
- Count bindings on the fully composed TSL graph, not per helper function. A deferred pass that invokes one helper for each of three cascades can bind each cascade's buffers independently; three buffers per cascade therefore becomes nine and fails pipeline creation.
- Prefer packing related data into an existing storage buffer, reusing an already-bound buffer/bit field, or moving read-only sampled data into textures. The probe and light data are intentionally packed for this reason.
- After any GI buffer or TSL sampling change, run the runtime WebGPU smoke test. A Vite build does not create GPU pipelines and cannot detect binding-limit failures:

  1. Start Vite locally.
  2. Run `node scripts/run-gpu-page.mjs http://127.0.0.1:<port>/scripts/gi-gpu-smoke.html 70000`.
  3. Require `GI-SMOKE PASS` and no WebGPU validation errors.

The recurring failure signature is:

```text
The number of storage buffers (9) in the Compute stage exceeds the maximum per-stage limit (8).
```

Treat this as a graph binding-budget regression, not as a reason to raise device limits.

## Test index

Not exhaustive yet — `package.json`'s `scripts` is the full list. Entries land here
as they are added, newest first.

| Script | What it gates |
|---|---|
| `npm run test:instancer-motion` | The InstancerComponent "Motion" per-frame layer (`src/engine/instancerMotion.js`). There is deliberately NO volume parameter: the LAYOUT's own bounding box is the volume. Scroll drifts along the direction and folds each axis over that box (leave one side, re-enter the other; a flat axis — a disc's normal, a line's cross-section — cannot wrap and drifts freely), driven by the shared clock, not the call count; rotation spins in place around the axis at °/s with seed-deterministic per-instance speed jitter and never moves positions; boids run the Reynolds rules through a uniform hash grid (O(N), not O(N²)), are caged in the layout's box with flat axes given neighbour-radius thickness (a zero-volume cage would pin the flock to the layout's plane — pinned for by the "actually used the thickness" assert), with speeds clamped to [0.35·max, max]; a 100-second hitch dt is clamped to 0.1 s everywhere — including the boids integration, which once saw the raw dt while the clock saw the clamped one and flung the flock (the regression this file exists to catch). |
| `npm run test:instancer-reload` | **Instances must not become planes after a scene reload.** Components attach synchronously in stored order, so an InstancerComponent attached after a MeshComponent builds its InstancedMesh from the PLACEHOLDER primitive while the real `.geom` is still a promise in flight; the swap (`#announceSwap("geometryAsset")`) is the only announcement. This pins the component-changed contract: geometry keys REBUILD the instancer, a material swap refreshes the material WITHOUT a rebuild (it fires on every .mat notification), noise keys and other entities are ignored, a detached instancer stays dead, an instancer added before its mesh builds once the mesh arrives, and motion parameter edits are read live rather than re-allocating the instance buffer per pointermove. |
| `npm run test:post-bundle` | The post-process stash/adopt pipeline cache is only as good as `PIPELINE_BUNDLE_FIELDS` being COMPLETE: a stashed bundle must be a restorable WHOLE pipeline. The first version omitted the editor overlay pass — but `#applyEditorHelpers` bakes that pass's textures into the bundle's `outputNode`, so the next camera's build disposed it and the adopt later restored a pipeline sampling a disposed PassNode (and a stale `_passCamera` made the next build throw away the freshly adopted scenePass). No GPU can run these paths headlessly, so this pins the invariant structurally: every field the dispose paths null is on the list, the overlay state + `_passCamera` + `keepaliveTemps` travel, the stash nulls fields BEFORE withdrawing `engine.scenePass` (`#unpublishScenePass` only fires when `this.scenePass` is already null), and a camera swap disposes with `keepStashes: true` and tries adoption before rebuilding — swapping cameras must never destroy the other camera's good bundle. It also pins the SELF-HEAL that fixes "post missing after load AND in the build, until an edit forced a rebuild": a load-time build that aborted (generation bump mid-await) or REJECTED (`loadAddonsForGraph` losing to the boot fetch storm — every call site `void`s it) used to leave the component permanently pipelineless; `ownsCamera` (which runs every frame the engine consults its overrides) now kicks a throttled rebuild for an eligible-but-pipelineless camera, and `#ensurePipeline` catches instead of leaking the rejection. And it pins the GODRAYS ROOT CAUSE found live on the user's scene: god rays compile against a shadow-mapped light SNAPSHOT taken at build time, but at boot that light has no map yet — the node compiles marching nothing, and since nothing re-ran the resolution it stayed dark until a param edit forced a rebuild (and forever in the preview build, where nobody edits); `render()` now polls while `_godraysAwaitingLight` is set and rebuilds with reason `godrays light appeared`, which MUST bypass the hot-param early return (identical signature by construction — observed live: the watcher fired, the flag reset, the early return ate the rebuild, god rays stayed dark). Diagnostic rigs: `scripts/run-post-diag-probe.mjs` (boots a fixture or `PROJECT=` real project and reads the live component state) and `scripts/run-post-build-diag.mjs` (reads a running preview build's `__engine`). |
| `npm run test:simulation-suspend` | **A modal editor mode stops the rest of the editor.** Entering the geometry editor mounts an opaque overlay over the viewport and draws through its OWN renderer and OWN rAF loop — yet the engine used to keep ticking and rendering that hidden canvas at full rate: the water solver's FFT chain every frame (water does not even take the `isInView()` gate), GI's g-buffer prepass re-rendering the whole scene, batching/merging/impostors re-grouping, the selection outline compositing. `Engine.suspendSimulation(reason)` is the seam and this pins what a rename or a refactor would silently break: it is REF-COUNTED BY REASON so two modal holders cannot resume each other, an unbalanced or unknown resume is a no-op rather than an underflow that un-suspends a live hold, `simulation-resumed` fires once when the LAST hold goes, and the guard every consumer uses is literally `engine.simulationSuspended === true`. ⛔ It is deliberately NOT `renderSuspended` — that flag is GI's, and suspending the DRAW while preRender still runs re-creates the ShadowFreeze latch in `Engine.#tick` (a light's map switches off for the session). ⛔ And it is NOT `Entity.reconcileActivity`/`_attached`, which is authored, serialised, undoable state that tears down GPU resources the geometry editor is BORROWING. The pacing side lives in `editorFramePacing.js`, which stops the engine loop outright for the whole session. |
| `npm run test:gi-variant-key` | The compile wave walks ONE object per pipeline VARIANT, and this pins where that key forks. It used to key on `material.uuid` — the one field three's `RenderObject.getMaterialCacheKey` explicitly skips — so every material INSTANCE was a variant and the wave paid a node-graph build plus a `createShaderModule` over 180-250 kB of WGSL for each duplicate. The key now mirrors three's: `customProgramCacheKey()` (where node identity AND GI's roughness bucket enter), the same property walk with the same normalisation (numbers to on/off, `side` kept exact as the enum it is, textures to mapping + sampler state), the geometry attribute/morph/index key, bone count, instancing, `receiveShadow`. ⚠ MERGING is the dangerous direction and is why the file exists: two objects on one key means one is never warmed, nothing reports it, and the editor freezes on a synchronous pipeline creation the first time the camera turns onto that material — the exact failure the wave and `asyncRenderPipelines.js` both exist to prevent, arriving by a new road. So every fork three has is asserted against hand-built objects (no GPU needed), including the ones that are easy to lose: attribute ORDER must NOT fork (three sorts them), a skinned mesh forks on BONE COUNT rather than skeleton identity, every InstancedMesh is its own program because three appends `object.uuid`, and a material whose cache key THROWS falls back to per-instance rather than merging two unknowns. `__giWaveVariantKey = "uuid"` is the A/B arm. |
| `npm run test:gi-compute` | GI's compute dispatch (`giCompute`) must never throw from its OWN bookkeeping. It wraps every dispatch in a `try/finally` that closes a freeze span, and it sits on the hottest path GI has — `#refreshDynamicObjects` calls it every tick, `#compileWave` throughout a rebuild. ⛔ On 2026-09-07 the span token was declared `const` INSIDE the `try` and read in the `finally`, which is a SIBLING scope: every dispatch threw `ReferenceError: kernelSpan is not defined`, the console filled with hundreds of identical stacks, and GI was never committed. A throwing `finally` also REPLACES the real error, so the one message that could have named a broken kernel never appeared. `node --check` cannot see this, and neither can a brace-depth scan — a `try` and its `finally` sit at the same depth, which is why the heuristic guard written for it was thrown away and the real function is driven instead. Asserts the dispatch reaches the renderer, that an array goes one node at a time (an array dispatch attributes every pipeline it creates to the ARRAY and loses the per-pass name), that nulls are skipped, that a FAILING dispatch surfaces its own error rather than a ReferenceError, and that the span stack is balanced afterwards — including the already-built path where the token is 0 rather than a handle. Verified to fail all five checks with the bug reintroduced. |
| `npm run test:async-pipelines` | **Defer a FIRST compile; never defer a RE-MINT.** `installAsyncRenderPipelines` routes big pipelines through `createRenderPipelineAsync`, and three SKIPS the draw entirely while `_pipelines.isReady()` is false. So replacing an already-drawing object's pipeline with a compiling one makes it VANISH until the driver finishes — reported 2026-09-07 as "every mesh I select disappears for a moment, and then gets back", reproduced by selecting one mesh in Sponza (its material rebuilt on `aoNode,colorNode,customProgramCacheKey,emissiveNode` and a new `renderPipeline_MeshPhysicalNodeMaterial_138 [async]` appeared). A first compile has nothing on screen, so deferring is free; a re-mint trades a visible object for an invisible one, which is a rendering bug. ⛔ Checking the previous pipeline INSIDE `_getRenderPipeline` does not work — `getForRender` releases it first — and that version passed a simpler test while changing nothing live, so the fake here reproduces the release-then-create order on purpose. `__asyncRenderPipelinesRemint = true` is the A/B arm. |
| `npm run test:geometry-islands` | **Splitting a mesh into its disconnected pieces** (`geometry.splitIslands`). An import routinely packs unrelated surfaces into one mesh — Sponza ships its curtains three and four to a `.geom` — and every per-object decision is then made for the GROUP: one cloth component for three curtains, one collider, one enabled flag. Anything DERIVED from the mesh is derived from all of them at once, which is a bug class of its own (one thin curtain dragged every other curtain's cloth contact radius to half what it needed). ⛔⛔ ISLANDS COME FROM WELDED POSITIONS, NEVER FROM INDICES: a model with UV seams stores the same point twice (that curtain is 7 739 vertices for 7 174 positions), and index connectivity would call every seam a piece and shatter one curtain into ribbons. ⚠ A quantised hash key ALONE is not a weld either — two points a fraction of an epsilon apart can land either side of a cell boundary and never be compared, so the epsilon is enforced by a 27-cell search. Both pinned. ⚠ It DEFAULTS TO A DRY RUN because of the data: `Mesh_0_6` is 28 pieces and `Mesh_0_1` (the vines) is **671**. ⛔ The glue bug unit tests could not see: `loadGeometryAsset` returns a BufferGeometry, not the definition, so the splitter got an object with no positions and reported a confident "single connected surface" for a mesh that is three — a wrong ANSWER, not an error. |
| `npm run test:cloth-contact` | **A CPU model of the cloth contact — built because three fixes shipped without one.** `contactPass` mirrors `projectClothMeshContact` minus the BVH. ⛔⛔ It CORRECTED the diagnosis: a particle further behind a wall than the contact radius is not pushed back to the wrong side, it is NOT TOUCHED AT ALL — the swept test finds no crossing and the face test finds it outside the slab, so contact abandons it and the spring to its neighbours in front spans the wall for good. The fault is DETECTION, not side selection, which no amount of reasoning about `side` could have reached. ⛔⛔ It also carries the fixture whose absence cost a live regression: a MIS-WOUND triangle, which is why taking the side from the winding wrecked even the pristine island (worst spring 0.30 -> 17.25) — and it only bites where the bad face is the ONLY one covering the particle, so a test at the quad's centre passes and proves nothing. The shipped fix (`__clothSafeSweep`) sweeps from the last position contact CERTIFIED rather than from `previous`: identical in ordinary motion, recovers a stranded particle, keeps cloth authored under a floor under it, and consults no winding. |
| `npm run test:cloth-health` | **Is this piece of cloth still cloth?** ⭐⭐⭐ MOTION COHERENCE DECIDES, because every other reading describes ONE FROZEN FRAME — a cloth the right size in the right place with springs near rest scores perfectly while BUZZING, which is exactly what "fighting themselves" means and what a whole session of "the numbers improved" walked past. `coherence = |mean displacement| / mean |displacement|`: a curtain in wind moves far and moves TOGETHER (0.96-0.99), a cloth pulling against itself moves just as far with its particles disagreeing so the vectors cancel (0.56 measured on the island that was visibly wrong). Speed alone cannot separate them. ⛔ The shape readings had THREE blind spots, each shipped before being caught — the DIAGONAL called an 18-fold blow-out "plausible"; the MAXIMUM in-plane ratio called a curtain bunched to 64 % "healthy"; and extents cannot tell BLOWING from CRUMPLED at all. ⛔ And the first thresholds (0.05 mean / 2 worst) CONDEMNED EVERY HEALTHY CLOTH: they were read off one unusually relaxed island before it was known that a hanging cloth carries its own weight by stretching — forcing 3x the solver iterations barely moved it (0.060 -> 0.057). Now 0.25 / 3.5, from measured populations, and the test states plainly that the worst-spring margin (healthy spikes to 2.54, broken sat at 4.70) is narrow. |
| `npm run test:cloth-topology` | **Which meshes can be cloth, and what the solver gets when they can.** `clothMeshTopology.js` is the CPU half of draping an arbitrary mesh (asked 2026-09-07: "turn sails into cloth", skipping meshes like a human model). It welds seam duplicates (Sponza's curtain: 7 739 render vertices, 7 174 distinct positions — unwelded, the sheet tears along every UV seam on frame one), splits connected islands (that curtain is THREE separate 2.3 m drapes in one asset, and a boat's two sails must each be pinned on their OWN top edge), builds the constraint graph in CSR (structural = mesh edges at their real rest lengths; dihedral = the pair opposite each shared edge, which on a grid lands exactly where the old solver's shear diagonal was), and decides suitability. ⛔ Thinness is measured on PRINCIPAL axes, never a bounding box: a sheet rotated 45° has an axis-aligned box of 2.1×2.1×3 and would be rejected as solid. ⛔ The pin band is a fraction of the PINNED axis — sizing it to the piece's overall extent fixed a horizontal sheet pinning all 23 of its vertices and broke an 11 m banner into pinning 36 % of itself, so the flat-axis case is DETECTED and reported instead. Every rejection carries a reason that says what to do. Verified to fail with welding disabled and with the thinness gate removed. |
| `npm run test:gi-light-tree` | A lamp has not MOVED just because float32 cannot hold its address. `#refreshLightTree` caches each emissive mesh's world matrix in a `Float32Array` and rebuilds when one differs; a mesh that reads as moved also calls `srcProbes.invalidateVisCache()`, throwing away GI's world visibility cache. ⛔ The compare was an ABSOLUTE `> 1e-5`: float32 carries ~7 significant digits, so above ~256 m the representable quantum is already wider than that gate and the round-trip error is CONSTANT — a stationary lamp reports motion on every frame, forever. Measured over 200 000 coordinates in 256-768 m, **50.8 % trip it**. On the user's Level scene (502 m span): 2 554 refreshes and 2 554 invalidations with `camMotionEma` 1e-72 and `worldRested` true — GI was not initialising slowly, it was restarting 60 times a second and could never converge. Checks are written in float32 because the storage IS the mechanism; a double-based test passes against the broken version. `__giLightTreePoseEps` is the arm. |
| `npm run test:freeze-ledger` · `npm run probe:freezes` | **The instrument every zero-freeze unit is scored on** (`src/engine/freezeLedger.js`, `docs/ZERO_FREEZE_PLAN.md` Stage 0). The editor now records every main-thread BLOCK the browser reports as a long task and joins it to a ring of activity spans the engine writes as it works, so a freeze reads as an owner (`gi:rebuild/staticBvhBuild(SAH)`, `material:nodeBuild <name>`, `gpu:shaderModule`, `event:hierarchy-changed`, `scene:instantiate`) instead of a mystery — `profile.freezes`, `profile.boot` and `profile.edit` are the three ops that read it. The CPU test pins the properties a wrong instrument would quietly violate and this project has already been burned by: SELF time excludes children (or one block reports 300 % of itself and the specific owner hides under its callers), a span straddling the window edge is charged only for the part inside, unmarked time is reported as `(unattributed)` rather than redistributed onto whoever was on the stack, a `begin` whose `end` is skipped does not corrupt the stack for everything after it, and sub-millisecond spans never reach the ring. `probe:freezes` is the harness gate: it boots a real project (WITHOUT clicking the hub — a click navigates the page and every later `page.evaluate` dies with 'detached Frame', which is what killed the previous boot probe), prints the boot stage table, the blocks by owner, and every pipeline that was compiled SYNCHRONOUSLY with its name. ⚠ Never run it beside the user's editor: they share one GPU and one driver compile queue. |
| `npm run test:water` / `npm run test:water-material` / `npm run smoke:water-surface` / `npm run smoke:water-authoring` / `npm run smoke:water-appearance` | Water is a Plane Mesh with a forkable built-in Water material. Actual Rapier tests distinguish buoyancy by volume/density, not mass alone; waves share CPU/GPU noise, body wakes propagate, and caustics move on submerged receivers and turn off without their light. Material compilation and generated-code parity, actual first-edit fork/undo, isolated transparent-water GI reception and object reflections are gated. Fresh Vite5307, isolated profiles. Required generic GI smoke must also pass after GI changes. |
| `npm run test:cloth-gi` / `npm run smoke:cloth-gi` / `npm run smoke:gi-grid-bvh` | Cloth contributes color bounce using GPU-refitted transport geometry, rather than only receiving GI. The floor capture hides source cloth and direct sun, and compares graph red against neutral with stale scalar white, preventing source pixels or scalar albedo from faking the result. BVH smoke checks actual GPU vertices and conservative bounds after deformation for both packed arities. Fresh Vite5307; required generic GI smoke must also pass at portable8. |
| `npm run test:cloth-anchors` / `npm run smoke:cloth-anchors` / `npm run smoke:cloth-anchors-ui` | Entity-reference cloth pins follow transformed moving targets, coexist with preset pins, release on target deletion, and update without restarting the solver. GPU test uses nonuniform parent/source transforms and zero-delta updates; UI test uses actual entity/UV/offset fields plus keyboard undo/redo. GPU fixture on5307; fresh UI Vite5321. |
| `npm run test:query-lang` · `npm run smoke:query-search` | The shared query language behind every search box (`queryLang.js` + `queryEvalEntity.js` / `queryEvalAsset.js`), and the two panels plus Ctrl+F that speak it. The grammar: `Lamp...` prefix, `...Box` suffix, `?prop=value` filters joined by `&`, quoted names, whitespace-separated terms ANDed — plus the three shorthands the long forms kept getting wrong. A filter needs NO `?` when it stands alone (`mesh.castShadow=true`, `collider.shape=convex`), gated on a DOTTED path or a field the evaluators actually expose so `a<b` stays the substring it always was. A filter with no value is an EXISTENCE test (`M...?cloth`, `?!collider`), and the asymmetry that keeps the degradation guarantee alive is that `?width>` HAS an operator, is merely half-typed, and still degrades to text. A standalone `>` SCOPES: `Mesh > light` is every light descended from something named Mesh, it chains, and only the right-hand matches are returned — whitespace on both sides is the entire rule that keeps `light.intensity>1` a comparison. Scoping lives in `hierarchySearch.scopePool`, not the evaluator, because "inside" is a question about the TREE and a per-candidate matcher has none; the panel, Ctrl+F and the MCP op all call it, so none of them can invent a second meaning of "inside". The claim the CPU tests exist for is that a PLAIN string still behaves exactly as it always did — `"red lamp"` is ONE substring, not `red` AND `lamp` — and `hierarchy-search-query.test.mjs` re-runs the legacy loop verbatim as the reference. It also pins the drift this whole module exists to prevent: `selection.selectMatching` over MCP and the panel's filter box are the two callers of `matchCandidate`, so the plain/structured ROUTE lives inside it rather than at each call site; routing per-site is how the op silently started matching an entity named "lamp red" that the box does not. `asset-meta-index.test.mjs` drives the lazy on-disk metadata (`assetMetaIndex.js`) with injected IO and a COUNTED read, because every claim worth making about it is about how little it opens: a width query opens images and atlases and NOT every `.mat` in the project, the second identical search reads nothing, an edited file is re-read off its `size:modified` stamp, a broken file is a negative entry with a cooldown rather than a throw, and the version counter bumps per BATCH — a single bump at the end makes a long search look like it found nothing until it finished. The smoke is the half a unit test cannot reach: the real inputs, the real ranking in the panel (search mode renders `sortMatchIds`, so prefix hits lead and the tier-1 rest sorts by NAME, not scene order), Ctrl+Shift+A selecting exactly what is shown, and a recent search REFILLING the box instead of closing the dialog. It also drives the three shorthands and the scope operator through the real box against a rig with a lamp nested TWO levels down — a one-level fixture would pass with a direct-children implementation — and pins that a half-typed `Rig >` shows the left half rather than emptying the panel. Needs a fresh `npx vite --port 5262 --strictPort`. |
| `npm run test:editor-prefs` · `npm run smoke:inspector-collapse` | Folding inspector sections (`inspectorPrefs.js`, `InspectorPanel.jsx` → `SectionFoldHeader`) and the shared recent-searches list (`searchRecents.js`). Both stores load from localStorage AT IMPORT TIME — that is the whole reason a reload paints the fold rather than opening flat and snapping shut — so the CPU tests install the storage stub before importing, and cover the corrupt/hostile payloads (only an exact `true` is a fold this module wrote) and a storage that throws. The smoke owns three claims nothing headless can make. **The fold is keyed by COMPONENT TYPE**: folding "Light" on one lamp leaves it folded on the NEXT lamp you select, which is the papercut the feature exists to remove and is a claim about two renders of two entities. **No flash**: a MutationObserver installed before the inspector has anything to show, asserting no committed frame ever held the folded section open. **It still LOOKS like a header**: both hit targets are real `<button>`s (the header cannot be ONE button the way `.section-header.toggle` is elsewhere — the component header already nests the view-only / enable / remove cluster), so without `.section-title-hit` / `.section-collapse` in `theme.css` they arrive with the UA's own 13px sentence-case font, background and border and the title row reads as two grey chips. The smoke reads computed style for exactly that, plus that the chevron turns and that a folded section is ~31px rather than the 180px `content-visibility` placeholder that would make the scrollbar lie. Same fresh `npx vite --port 5262 --strictPort`. |
| `npm run test:cloth-colliders` / `npm run smoke:cloth-concave` | Cloth contacts actual Concave/Mesh triangles, preserving holes instead of substituting bounds. CPU tests cover cooked geometry, hierarchy transforms, caching and explicit triangle-budget errors. Real WebGPU smoke checks transformed thin surfaces, open gaps, moved colliders and fast sweeps at the portable eight-buffer limit. Fresh Vite on 127.0.0.1:5307; isolated browser profile. |
| `npm run test:particle-colliders` / `npm run test:vfx-stack` | Cloth is a plane-mesh modifier: no independent material/appearance/dimensions, borrowed material identity survives edits and detach, missing planes produce no surface, mesh changes rebuild safely. Collider tests use actual entities/components to gate auto-fit, auto-center, local rotation, signed world scale, convex bounds, proxy exclusion and enabled hierarchy/sensor filtering. Cloth GPU tests cover sustained wind with gust zero, world-down gravity, actual scene contacts and source-material pixels. Convex contacts approximate oriented bounds; capsules use enclosing spheres. |
| `npm run smoke:vfx-gi` | Actual SRC GI reception on particles, cloth and water, measured by rendered pixel differences with the GI light on/off and no authored reflection probes. Also exercises reflection capture activated by sharp water and the portable eight-buffer limit; `?level=1` tests the highest quality. Requires fresh Vite --host 127.0.0.1 --port 5295. The generic required GI smoke must pass too. |
| `npm run test:vfx-timeline` / `npm run smoke:vfx-workspace` | Four separate Particles/VFX/Cloth/Water modules. CPU gates animation interpolation, parent cycles, transient runtime lifecycle/export/migration, and particle numeric socket precedence/hot updates. Workspace smoke creates each through the real hierarchy, edits particle socket constants/wires, and exercises VFX timeline timing/keys/playback/undo. Fresh Vite localhost:5307, isolated profile. `smoke:vfx-graph` aliases this current editor smoke; the old mixed surface-node UI is retired. Surface GPU smoke additionally gates fabric textures, pin modes, cloth box/sphere contacts, driven waves and actual water transmission over submerged geometry. See `docs/VFX_SYSTEM.md` for solver limits. |
| `npm run test:vfx-assets` / `npm run smoke:vfx-assets` | Reusable `.vfx` particles/cloth/water documents. CPU tests gate validation, load races, preserved inline fallback, reroutes and exported dependency paths. Real editor smoke gates shared disk updates with independent GPU state, React Flow Save, particle reroute spawn readback, relative/absolute aliases and scene disk reload for all three kinds; zero console/page/WebGPU errors. Requires fresh Vite localhost:5293 and uses an isolated Chromium profile. |
| `npm run test:vfx-graph` · `npm run test:vfx-stack` · `npm run smoke:vfx-particles` · `npm run smoke:vfx-simulations` | Particle graph and surface runtime compatibility (`docs/VFX_SYSTEM.md`). CPU gates preserve graph metadata and validate legacy surface connections, presets and recipes. Particle smoke measures gravity/collider readbacks and compiles light/shadow/self-collision paths at the portable eight-storage-buffer limit; Vite localhost:5287. Surface smoke checks cloth pins/contacts, water motion/transmission, lit pixels, normals, restart and disposal; Vite localhost:5283. Both use isolated Chromium profiles. The current editor gate is smoke:vfx-workspace above. |
| `npm run smoke:level` | The blockout tools in a REAL viewport — the half `test:level` cannot reach: that a press-drag-release on the canvas creates a wall. The armed-tool gate, the CAPTURE-phase pointerdown that has to beat the selection picker to the event, the ray against the draw plane, and the Level + storey created on first use only run here. Also that a second piece joins the level that already exists rather than spawning a second one, and that disarming gives the viewport back. **Alt-drag** is exercised too: the left button is OrbitControls' ROTATE and an armed tool takes it, so without an escape the camera cannot be turned at all while drawing (reported as "we can't move the camera") — the smoke asserts an Alt-drag turns the camera AND creates nothing, and that middle-drag is remapped to rotate while armed. It also pins the material claim: a piece assigns its greybox material in `onAttach` and MeshComponent finishes its own async pass ONE MICROTASK later, which used to reset every piece to the default white with no `component-changed` to say so — a level drawn entirely in white with nothing in the console. `mesh.userData.materialOwner` is what stops it, and the smoke asserts the drawn wall's material is still `Blockout …`, tinted `f5c542`, with the metre grid map on it. **Undo/redo is pressed for real** (Ctrl+Z / Ctrl+Shift+Z through the keyboard, not commandBus.undo()) because the chord has to survive keyScope, the level tool key dispatcher and EditorChrome before it reaches the bus — driving the bus directly would pass while all three were broken. The claim it pins: ONE gesture is ONE Ctrl+Z, so undoing the first piece drawn in an empty scene takes its auto-created Level and storey with it (markGroup/collapseFrom in createPiece) rather than leaving an empty Level; redo restores them under the same ids and the tool re-adopts the level; and a level setting written through the bus pulls back into the toolbar mirror instead of the palette lying about the value it will draw with. Needs a **fresh** `npx vite --port 5201 --strictPort` (a stale server makes the harness's `import("/src/…")` a second copy of the app with its own Engine — see run-editor-ui-smoke.mjs). |
| `npm run test:level` | Level blockouts and the character that walks them (`modules/level-design/`, `editor/blockoutDraw.js`, `editor/characterRigSpec.js`) — see `docs/LEVEL_DESIGN.md`. Three layers whose failures are PICTURES rather than errors. **What a piece is**: a slab hangs BELOW its origin (its top face is the walkable elevation — get it backwards and every wall floats a slab-thickness off the floor), a wall's openings cut into up-to-four boxes rather than a CSG subtraction, an opening as wide as its wall leaves an archway instead of throwing, and a staircase's last tread lands exactly on the storey above. **What a gesture means**: both yaw formulas, checked by rotating the returned piece and asserting its far end lands on the drag's end point — a wall runs along local +X and a stair climbs along local +Z, so they cannot share one "yaw from a direction" helper, and an angle 90° out looks deliberate in a screenshot. Also that angle snapping applies ONLY with the grid off (snapped endpoints already quantise the angle; re-snapping pulls the wall off its corner). **That it is walkable**: a live engine with Rapier drives a real character onto a blockout slab, up an 8-step staircase, and through a wall's doorway — the end-to-end claim the whole feature makes. Plus the rig's feet-at-origin invariant and that both shipped `.ts` controller templates still compile the way `assetLoader` loads them (decorators on). Plus the WINDING gate: every triangle of every shape is checked against its own shading normal, because a backwards winding leaves normals, bounds and triangle counts all correct and only shows up as front-face culling letting you see through a piece into its far inner wall — how columns and ramps shipped inside out. ⚠ The staircase check reads the HIGH-WATER mark, not the final Y: keep walking past the top and the character crosses the slab and steps off its far edge, which reads as "never climbed". Also covers the third-person camera (rigid orbit — a flick of the mouse lands on its new arc the SAME frame, follow damping trails only the pivot, a boom arm between character and camera still cancels the body's rotation correctly), the FOV-kick `setProp` storm guard (an unguarded per-frame `setProp` re-mirrors the whole scene for React every frame — `FOV settles and STOPS writing`), and the default animated body's locomotion graph (`character animator` section: drives the REAL `AnimatorRuntime` against the actual shipped `CHARACTER_LOCOMOTION_ANIM` object with fake clips — Idle/Walk/Run blend weights at the controller's default speeds, the blend sums to 1, Jump↔Locomotion on the `Grounded` parameter). Also the input SIGN conventions: gamepad sticks are flipped to y-up at the device (`GamepadDevice.poll` — the Gamepad API is screen-space, and unflipped it was the one vec2 reading the other way, so stick-up walked backward), stick look is signed per device (a mouse delta is y-down and needs negating; a stick already reads up = +1, and the single flip shipped there had the touch stick pointing DOWN with Invert Y as the only fix — `pushing a stick up looks UP`, `gamepad sticks read y-up`), stick deflection normalizes to FULL pace above a 0.2 dead zone so a drifting thumb can't slow the character or its animation (`Analog Speed` restores the throttle), and two LIVE physics gates re-prove that queries accept a THREE.Vector3 origin (`physics queries accept a THREE.Vector3`, `Avoid Walls pulls the third-person camera in front of a wall` — a query indexing `origin[0]` on a Vector3 fed Rapier undefined and silently never hit, which is how Avoid Walls shipped passing through walls). No GPU, ~10s. |
| `npm run test:sky` · `npm run smoke:hdri` · `npm run smoke:skybox` | The scene's sky (`engine/environmentAsset.js`), which takes **either** a `.cubemap` **or** an equirect `.hdr`/`.exr` in ONE slot — `settings.environment.cubemap`, whose name is historical. The unification is the point: an imported HDRI used to spawn an `environment` COMPONENT on an entity, so the viewport showed an HDRI while Scene Settings said "Cube map: None" and none of its knobs (intensity, rotation, sky blur) reached it. `test:sky` is the headless half — extension dispatch, and that a load NEVER rejects (a moved file must leave the flat background, not throw out of a settings apply). `smoke:hdri` is the real-WebGPU half, and gates the failure that is a picture rather than an error: an equirect assigned without `EquirectangularReflectionMapping` renders as a stretched rectangle, and a v-flip renders upside down, so it paints the panorama's top half red and its bottom half green and looks up and down. It also carries a trap worth knowing before you shrink its fixture — **PMREM derives its cube size from the source image, so a toy 4×2 panorama lights a white sphere BLACK while the sky still draws perfectly**, which looks exactly like a broken IBL path and is not one. `smoke:skybox` is the six-face sibling, including three's X-mirroring (`tFlip`) face-order convention. Both smokes need `npx vite --port 5209 --strictPort` and a real GPU. |
| `npm run test:post-ssr` | The SSR post node's reach (`modules/postprocessing/postGraph.js`). **`SSRNode.maxDistance` is `uniform(1)` — one WORLD UNIT** (SSRNode.js:168), and the builder never assigned it, so the effect reflected a footstep of floor and nothing beyond. The first test pins the addon's own default so a three upgrade can't quietly move the premise. Also gates the composite's contract: SSR hands back a contribution PREMULTIPLIED by `(1 - d/maxDistance)²`, so a hit near the range limit is a *black* sample, not an absent one — mixing beauty toward it on a binary `alpha > 0` mask painted the surface black over exactly the band where the GI/env fallback should have taken over. And the hot-param path: `updateParams` was a stub while the component early-returned on signature equality, so **every numeric post param was inert** until some structural edit forced a recompile — the test drags the sliders and asserts the uniforms move, that the signature does NOT, and that a cleared prop returns to its default. Plus: SSR compiles downstream of another effect (the addon infers its camera from `colorNode.passNode`, which only the raw scene pass carries, and throws otherwise). And the one that actually made a mirror black: **the matParams MRT must follow `metalnessNode`/`roughnessNode`, not the scalars** — `TSL.metalness` is a `materialReference` to the JS scalar, while `MeshStandardNodeMaterial.setupVariants` prefers the node, so every shader-graph material (graph metalness 1 over a stale asset scalar of 0) reached SSR as a rough dielectric and the addon's `metalness <= 0` discard threw away all of its pixels. GI hit the same disagreement on the same material (`giLight.js:1429`). No GPU, instant — the WGSL itself is not compiled here. |
| `npm run test:post-asset` | The `.post` document format (`modules/postprocessing/postAsset.js`) — the file a camera's Postprocess component renders through, and the reason a look is authored once instead of copy-pasted between cameras. The module deliberately imports no `three`, which is what lets this run in plain node: by the time a malformed graph reaches the TSL compiler the error names a node, not a file. It gates the two ways a hand-edited or agent-written `.post` goes wrong without erroring — a **dangling edge** (delete a node in a text editor and its wires stay behind; the compiler walks edges without checking both ends exist), and an **empty or unparseable graph**, which must fall back to the passthrough rather than to nothing, because a black viewport with no message reads as a broken effect rather than as an empty file. Also pins that the shared `DEFAULT_POST_GRAPH` is never mutated by a caller, and that a bare `{nodes,edges}` still loads — lifting an inline graph out of a `.scene` by hand is the obvious first thing anyone tries. The live half is `node scripts/run-mcp-tools-smoke.mjs` (the `post.*` ops against a real editor) and the panel half is `node scripts/run-editor-ui-smoke.mjs`. |
| `npm run smoke:startup` | What happens between launching the editor and a project being open (`src/editor/startupReopen.js`, `main.jsx`). The editor reopens the last project and walks past the project hub, because "which project?" is a decision, and re-asking it every launch of a tool someone uses on one project for weeks is a toll rather than a choice. Every way that goes wrong is **silent** — an editor that opens the wrong thing, or stops at a picker it should have skipped, throws nothing — so the smoke gives each launch condition its own page rather than inferring them from one boot. The case that matters most is a remembered project that has been MOVED OR DELETED: `openProject` reports success for a path that is no longer there (a `project.json` that won't read is a tolerated "not a hub project", and the folder listing catches its own error into store state), so without the existence check the user lands in an editor pointed at nothing and the only symptom is an empty Assets panel. That path is also deliberately NOT forgotten — an external drive that isn't plugged in yet looks exactly like a deleted folder. Two more it pins: **`__editorNoAutoOpen`**, which `installTauriShim` sets by default and which ~160 harnesses in `scripts/` depend on (most click "Skip the project"; the GI/blackframe family seeds a recent list and waits on `.hub-recent-open-btn`, so a launch that skips the hub would hang them on a selector that never appears), and that **"Skip the project" actually skips** — the Assets panel used to call `restoreLastFolder()` on mount whenever no project was open, which reopened the last project a beat after the user chose not to, and went unnoticed only because a harness profile has no last project to restore. ⚠ localStorage is per-ORIGIN, not per-page, so the harness wipes storage in `evaluateOnNewDocument` — without it, case 1 opening a project makes case 2's "first-ever launch" silently test the opposite of what it says. Needs `npx vite --port 5213 --strictPort`. |
| `npm run test:occlusion` | Hi-Z occlusion culling (`engine/culling/`). Most of it asserts that things are still DRAWN, because the failure mode is objects that vanish. The section to know about is **"the readback arrives in the pyramid's row order"**: `projectSphere` maps NDC y to `v = y*0.5+0.5`, so `v` runs UP and `DepthPyramid.build` needs row 0 at the BOTTOM — which only `gl.readPixels` gives you. A WebGPU readback is top-down, and feeding it through unflipped mirrors every depth test about the screen centre: no error, no blank frame, just objects culled against whatever is on the opposite side of the view. That shipped, and was reported as "meshes disappear in front of the camera". The last check reproduces it — a wall across the bottom half, an object high on screen with only sky in front, culled by the un-flipped buffer and kept by the flipped one. Also gates that culling is configured on the **camera**, not the scene: `CameraComponent` owns `frustumCulling` / `occlusionCulling` / `occluderMinSize` / `occlusionBias` / `cullShadowCasters`, `occlusionCulling` is TRI-STATE (`inherit` keeps `settings.performance.occlusionCulling` meaning what it always meant, so no pre-existing scene changed behaviour), and the **editor viewport camera is not an entity** — it falls back to the scene's first enabled camera so the viewport culls the way the build will. No GPU; the depth PASS itself lives in `npm run smoke:occlusion`. |
| `npm run test:readback` | Reading a render target back as an image (`engine/renderTargetImage.js`), the one place that knows the two backends disagree about **both** row padding and row order. WebGPU pads rows to 256 bytes and `copyTextureToBuffer` preserves texture order (**row 0 = TOP**); WebGL packs tightly and `gl.readPixels` counts **from the bottom**. Both ways of getting it wrong return an image rather than an error — the wrong order is a perfect picture upside down, the wrong padding is a progressive shear — and the first one shipped: every `viewport.screenshot` and every `.geom` thumbnail was inverted, because WebGL's flip had been hand-copied into the WebGPU path. Also pins that the last row is SHORT by design (three sizes the buffer `(h-1)*paddedRow + rowBytes`) so a reader must clamp, and that an unknown backend is assumed to be WebGPU. Fake renderer, no GPU, instant. |
| `npm run test:geomglb` | `.geom` → GLB export (`editor/geomExport.js`), the conversion half — the save dialog and the write need Tauri and are not covered. Gates the three things that are wrong only once the file is already open in Blender: positions/normals/UVs/index/morph targets survive a real GLTFExporter → GLTFLoader round trip; **`geometry.userData` never reaches the file** (three calls `serializeUserData(geometry, primitive)`, and every mesh from Edit Mode carries `editMesh` — the whole polygon topology and per-corner UVs, usually bigger than the vertex data — plus `assetPath` and the GI ray proxy); and **material groups only survive as an ARRAY of materials** — hand the exporter one material and it silently emits a single primitive over the whole index buffer, welding a two-slot mesh into one. Needs a `FileReader` shim (Node has `Blob` and not `FileReader`, and the exporter's binary writer uses both). No browser, instant. |
| `npm run test:confirm` | The destructive-confirmation dialog (`components/ConfirmDialog.jsx`), which replaced the OS message box after that box was seen dismissing itself. Gates the two rules that make a delete prompt safe: it cannot be dismissed by the interaction that opened it (backdrop closes on **`click`, never `pointerdown`**, plus a one-frame arming guard — a `pointerdown` backdrop eats the pointerup of the menu item that opened it, which looks exactly like "the popup vanished"), and a stray Enter cannot delete (focus lands on **Cancel**; Enter only fires when the destructive button really has focus). Also that every failure mode answers *false*, and that `assetOps.js` no longer imports the Tauri dialog or `window.confirm`. No browser, instant. |
| `npm run test:time` | `engine.time` (`src/engine/time.js`) — clocks + scheduler. Gates the specific failure each structure avoids: a stale handle must not cancel the timer that inherited its slot (handles are `gen·2²⁰+slot+1` — the **`+1` matters**, without it the first timer ever created gets handle `0` and can never be cancelled); a repeater must not drift *and* must not fire 10,000 times catching up after a stall; game timers freeze under pause while real ones don't; a frame wait must survive the 256-bucket wheel wrapping; a cancelled `await` must RESOLVE `false`, not hang or reject; a callback may schedule/cancel from inside itself. Plus a measured claim: an idle frame with 20k pending timers costs the same as with none. No browser, ~1s. |
| `npm run test:library` | The seven asset browsers (Poly Haven, ambientCG, Sketchfab, Poly Pizza, KayKit, itch.io, Audio Library) as source contracts, plus the MCP asset flow. **Registration**: each panel wired into all five points (lazy import + component map + **dock entry** in `EditorShell.jsx`, `MenuBar.jsx`, `QuickSearch.jsx` — miss the dock entry and the tab opens EMPTY with no error), each module registered and categorised `Assets`. **Agent flow**: every browser reachable through an op family; `library.import` returns an actionable `primary` + `next` per asset kind, with the prefab FOUND by listing (a name collision suffixes the FOLDER while the prefab keeps the original stem, so string-building it misses); `audio.library.import`/`font.import` name their follow-up too. **Poly Pizza**: `x-auth-token` (Bearer 401s identically to no key), host allowlist, PascalCase response fields, the two ids (`PublicID` vs `ResourceID`), and — the big one — **all three filters are NUMERIC**: `Category` 0-11 in their array order, `License` 0=CC-BY/1=CC0 ("any" = omit), `Animated` 1/0. A string value is NaN and 400s; a lowercase param name is silently IGNORED and returns a full unfiltered page that looks like it worked. **KayKit**: GLB-only listing (loose `.gltf`+`.bin` trios and FBX are ignored), display names strip the `.gltf.glb` double suffix, ids are `<repo>:<path>` because the trees API has no get-one-file endpoint, character-vs-prop and the `animated` flag are PATH decisions (`/Characters/`), no credential anywhere, and keyless providers must stay out of `CREDENTIAL_PROVIDERS`. Plus the interactive `ModelPreview` (throttled, disposes its GLB, clip switching without re-downloading). No network, instant. |
| `npm run test:stats` | `engine.stats` (`src/engine/StatsSystem.js`): FPS is a COUNT of presented frames over a one-second window, not an EMA of `1000 / dt` (which overstated a hitching second by ~2×) and not a count of engine ticks (a `renderSuspended` wave used to report the full refresh rate for a frozen canvas). Also that the window ends at READ time, so a stopped loop decays to 0 instead of holding its last number, and the `PerfReadout` drift guard against `script-types/engine.d.ts`. No browser, instant. |
| `npm run test:math` | `engine.math` (`src/engine/math/`): the specific wrongness each function avoids — smoothing that is frame-rate independent, angle blends that take the short way, a seeded stream that replays, sphere sampling that is uniform, a ray from inside a volume reporting its exit, a ballistic solve that lands when you integrate it. Plus the **drift guard**: every member of the runtime `math` namespace must be declared in `script-types/engine.d.ts`, and `math` must reach all three of `import { math } from "engine"`, `engine.math` and `this.math`. No browser, ~1s. |
| `npm run smoke:script-rename` | A script file and its default-exported class keep the same name from either end (`scriptClassSync.js`): renaming the asset rewrites the class, renaming the class renames the file, other classes in the file are untouched, and open tabs + entity script slots follow. Needs a **fresh** `npx vite --port 5217`. |
| `npm run smoke:keyscope` | Keyboard ownership (`keyScope.js`): one context owns each key, the code editor keeps Ctrl+S/Ctrl+F/Delete, the app keeps Ctrl+P/Ctrl+B from inside it, and maximize is a double-click gesture that Escape does not undo. Needs a **fresh** `npx vite --port 5217`. |
| `npm run smoke:screenshot` | The Shift+Alt+S viewport screenshot (`viewportScreenshot.js`, the `editor.screenshot` binding in `keybindings.js`): a REAL CDP keypress through keyScope and EditorChrome's dispatcher writes a PNG into the folder configured at Project Settings → Screenshot, named `<prefix>-<date>_<time>.png` with PNG magic and a non-empty frame (the capture is the LIVE canvas readback — `readLiveCanvasImage`'s raw WebGPU `copyTextureToBuffer` — so a zero-byte or black file is a broken copy, not a missed key). The LIVE-canvas gate: a raw WebGPU fullscreen triangle in pure red registered as a post-render callback — the same slot the GI path tracer blits from, and invisible to any capture that re-renders the scene — must fill >90% of the readback, pinning WYSIWYG for debug views. Also the macOS gate: a synthetic keydown with `key: "Í"` — what Option+Shift+S reports on a US layout — but `code: "KeyS"` dispatched through the REAL window listener must produce a second file; `keyTokenFromEvent` reading the physical key is the whole reason Alt-chords work on Mac. That the visibility bindings sharing `chordMatches` still hold (H matches H, plain h does not match Shift+H). And the clipboard second delivery: both captures put the SAVED FILE'S PATH on the clipboard via `plugin:clipboard-manager|write_text` (shim handler standing in — there is no OS clipboard to read back), the exact absolute path of the first shot. Needs a **fresh** `npx vite --port 5279 --strictPort` and a real GPU (the capture reads a presented frame). |
| `npm run test:gi-src-surface` | SRC static surface attribution (`srcSurface.js`): a traced hit reads its own mesh's albedo, the stamp is deterministic across dispatches, the NEE emitter flag round-trips, a recolour costs no re-voxelize. Real occupancy field + real rays; race-sensitive, run 2-3×. |
| `npm run test:gi-src-shade` | SRC hit shading (`srcShade.js`) against `srcRef.js`'s `makeHitShader`. |
| `npm run test:gi-src-ref` | The SRC CPU mirror — no GPU, ~9s, the reference every SRC gate is diffed against. |
| `npm run test:gi-watchdog` | The §12.56 pending-pipeline watchdog can actually re-roll: three fills a MODULE-LEVEL SINGLETON descriptor and `.reset()`s it on the next line, so keeping the reference gave every retry `layout: null` and a 100% rejection rate — the watchdog logged recoveries it never performed. Fake backend/device, no GPU, instant. |
| `npm run probe:gi-debug-views` | The GI debug-view overlays compile and render without page errors: the legacy `occupancy` / `sdf` / `src-probes` volume views driven by `globalThis.__giDebugView`, the GI-term overlays (`indirect` / `ao` / `reflections`) driven by the inspector's `debugView` prop, AND `path-tracer` (three-gpu-pathtracer WebGPU, a live renderer rather than a buffer sample — `giPathTracer.js`). Gating is the absence of `pageerror` events across every arm. The new debug-quad path (`#buildDebugView`) is anchored in NDC via `vertexNode = vec4(positionGeometry.xy, 0, 1)` — `renderer.render(scene, camera)` traverses `scene`, NOT the camera's children, so a mesh parented to the camera is invisible; the legacy volume views live in world space, the term-overlay quad lives in the scene with an NDC-passthrough vertex shader. Path-tracer blits in post-render onto the canvas, so the live crop is the signal and `viewport.screenshot` (a separate raster) will not show it. Requires a fresh `npx vite --port 5201 --strictPort` and a non-zero `npm run smoke:gi-gpu` to confirm the binding budget after the term-overlay shader. |


⚠ A harness dying with `Failed to launch the browser process: Code: 0` and an
EMPTY stderr is not a broken harness — it is Chrome's DEFAULT PROFILE being
locked by a browser you already have open. Every one of the 189 puppeteer
scripts launches without a `userDataDir`, so they all share it. Verified fix:
pass `userDataDir: <a scratch dir>` to `puppeteer.launch`, or close Chrome.

From owner:
1) Use subagents to do work in parallel
2) Short replies: 2-3 sentences, main things
